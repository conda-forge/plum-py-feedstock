{% set name = "plum-py" %}
{% set version = "0.7.5" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/plum-py-{{ version }}.tar.gz
  sha256: 4b244255982c002d7996cfa7f9a203cdcf5c87c7f4d57397cb647ec8f2cfd51f

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3.6
    - pip
    - setuptools >=39.0.1
  run:
    - python >=3.6

test:
  imports:
    - plum
    - plum.bitfields
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://plum-py.readthedocs.io/en/latest/index.html
  summary: Pack/Unpack Memory
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ocefpaf
